<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".playlist.ui.playlist.PlaylistFragment">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCreateNewPlaylist"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/ys_display_medium"
        android:text="@string/newp_playlist"
        android:textAllCaps="false"
        android:textFontWeight="500"
        android:textSize="14sp"
        app:cornerRadius="54dp" />

    <TextView
        android:id="@+id/tvEmptyPlaylistPage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/SearchActivityErrorTextStyle"
        android:text="@string/you_did_not_create_playlists"
        android:layout_gravity="center_horizontal"
        android:textAlignment="center"
        android:drawablePadding="16dp"
        android:layout_marginTop="46dp"
        app:drawableTopCompat="@drawable/ic_empty_search_output"
        android:visibility="gone"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPlaylists"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="8dp"/>

</LinearLayout>